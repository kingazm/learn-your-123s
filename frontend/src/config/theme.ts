export const BRAND = {
  body:     'var(--brand-body)',
  bodyDeep: 'var(--brand-body-deep)',
  cap:      'var(--brand-cap)',
  capBand:  'var(--brand-cap-band)',
  label:    'var(--brand-label)',
  eye:      'var(--brand-eye)',
  blush:    'var(--brand-blush)',
} as const
